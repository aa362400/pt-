apiVersion: v1
kind: ConfigMap
metadata:
  name: shopmate-config
  namespace: shopmate
data:
  NODE_ENV: production
  LOG_LEVEL: info
  REDIS_HOST: shopmate-redis
  REDIS_PORT: "6379"
  DATABASE_HOST: shopmate-postgres
  DATABASE_PORT: "5432"
  DATABASE_NAME: shopmate
  CORS_ORIGIN: https://app.shopmate.ai
