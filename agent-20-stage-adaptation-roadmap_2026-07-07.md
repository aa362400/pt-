# 智能体 · 20 阶段平台适配与自治路线图

> 编写日期：2026-07-07
> 对象：`电商设计图保持产品一致性智能体`（Python Flask 双智能体）
> 目标：与 `后端`（NestJS）+ `智能体前端`（Vite/React）完美适配，并从"被动执行工具"升级为"主动、稳定、有记忆的平台数字员工"
> 基线：本文所有"已有/缺失"判断均核对过当前代码（2026-07-07 第三批修复后）

---

## 零、先回答你的八个问题（结论先行，细节在 20 阶段里）

| # | 你的问题 | 当前真实答案 | 达成手段 |
|---|---------|------------|---------|
| 1 | 为什么要适配？ | 平台调智能体走 `/api/v1/agent/runs` 异步任务协议。协议对齐了，但**能力面、事件流、身份、记忆**四层还没对齐——智能体现在不知道"是谁、在哪个组织、为了什么"在调它 | 阶段 1-4 |
| 2 | 怎么适配的？ | 已做：API Key 鉴权 + 9 种 taskType + 轮询取结果。未做：回调推送、租户贯通、契约测试 | 阶段 1-4 |
| 3 | 适配了什么功能？ | 已通：出图、产品分析、7 种文本任务（LLM 驱动）。未通：平台事件感知、平台数据回灌、代表用户操作平台 | 阶段 1-4、9-10、17 |
| 4 | 适配后能开始工作了吗？ | **能，但只是"你叫它才动"的被动工作**。配好 `AGENT_API_KEY` + 生图 Key + LLM Key 即可跑通全部 9 种任务 | 阶段 1 结束即可上岗 |
| 5 | 稳定吗？能 98% 不出错吗？ | **现在没人能证明**——没有压测记录、没有错误率统计。98% 成功率（错误预算 2%）是可达成的工程目标，但必须靠阶段 5-8 的重试/幂等/压测/故障注入来"测出来"，不是宣称出来 | 阶段 5-8 |
| 6 | 够聪明吗？ | 执行层聪明（场景导演式提示词、LLM 规划、机会雷达、竞品监控都是真实现）。**决策层不聪明**：不会拆解多步任务、不会自检结果、不懂你平台的业务上下文 | 阶段 9-12 |
| 7 | 能主动发现"现在该做这件事"、自动安排、自动处理平台所有功能吗？ | **完全不能，这是最大的差距**。它没有平台事件订阅、没有排程器、没有代表用户调平台 API 的通道 | 阶段 13-17 |
| 8 | 完事后能记住、不忘、下次直接干完吗？ | 记忆骨架已有（memory_store 分层记忆 + user_memory 用户画像 + knowledge_base 行业知识），但**只记"聊天里的偏好"，不记"干过什么活、结果怎样"** | 阶段 18-20 |

**一句话总结：适配层完成 ~60%，稳定性未验证，聪明度=执行强/决策弱，主动性=0，记忆=有骨架没闭环。走完 20 阶段，它才是你要的那个"自动发现→自动安排→自动干完→记住"的智能体。**

---

## 一、现状盘点（真实代码依据）

### 已经有的（别重复建设）
- **对接协议**：`web/routes/integration.py` — API Key 鉴权、`POST/GET /api/v1/agent/runs`、9 种 taskType（generate_images / analyze_product + 7 种 LLM 文本任务）、有界线程池 job_queue、状态落盘
- **执行智能**：双智能体引擎（Observer 规划 + Executor 执行 + 监督回路）、`commerce_llm` 场景导演式逐张提示词、一致性评分
- **业务雷达**：`opportunity.py` 选品机会评分、`competitor_watch.py` 竞品主图/标题变化监控、`knowledge_base.py` 行业知识注入
- **记忆骨架**：`memory_store.py`（AutoMem 分层经验卡）、`user_memory.py`（跨会话稳定偏好）
- **平台侧**：BullMQ 队列（并发/重试/死信）、ReviewTask 人审工作流、审计日志、通知系统、FeatureFlags

### 缺失的（20 阶段要补的）
- 契约测试与版本化（平台↔智能体接口漂移无人发现）
- 事件推送（现在纯轮询，3 秒一次，15 分钟超时）
- 租户/用户身份贯通（智能体不知道调用者是谁）
- 稳定性证据（零压测、零错误率统计、零故障演练）
- 任务规划器与自检器（多步任务、结果校验）
- 平台事件感知 + 主动建议 + 自动排程 + 授权护栏
- 工作记忆（干过什么、结果如何、下次怎么干得更好）

---

## 二、20 阶段路线图

### 波段 A · 完美适配（阶段 1-4，约 1 周）——"接得上、对得齐、认得人"

**阶段 1 · 接口契约固化** `P0`
- 做法：把 9 种 taskType 的输入/输出写成 JSON Schema（双端共享）；双端各加契约测试（NestJS 端 mock 智能体、Python 端校验请求形状）；接口加 `contractVersion` 字段
- 为什么：现在后端 `http-agent.provider.ts` 和 `integration.py` 的字段对应全靠人肉，改一边另一边就静默坏
- 验收：任何一端改字段，CI 契约测试立刻红

**阶段 2 · 事件推送替代轮询** `P0`
- 做法：智能体任务状态变更时回调平台 webhook（`POST /api/v1/agent-runs/:id/events`，带 HMAC 签名）；平台经 WebSocket/SSE 推给前端；轮询保留为降级路径
- 为什么：轮询 3s 间隔 × 15min 上限，前端进度条卡顿、长任务超时即失败
- 验收：ImageWorkbench 出图进度实时刷新（导入→分析→规划→逐张生成），断线自动降级轮询

**阶段 3 · 前端体验适配** `P0`
- 做法：ImageWorkbench 展示智能体真实过程产物（场景导演的背景/道具/光线/情绪卡片、一致性分、失败原因）；审核中心（ReviewCenter）直接展示原图 vs 生成图对比；文本任务结果结构化渲染（不是一坨 JSON）
- 为什么：智能体输出了丰富结构，前端现在只显示一个 URL——"适配"不只是接口通，是**用户看得见智能体的思考**
- 验收：出一次图，前端能看到每张图的场景设计卡与评分；驳回时能填意见并回传智能体重做

**阶段 4 · 身份与租户贯通** `P0`
- 做法：平台调用时透传 `orgId / userId / workspaceId / locale`；智能体按 org 隔离会话目录、记忆命名空间、用量统计；回传结果带同样上下文
- 为什么：现在所有组织共用一个智能体"大脑"，A 公司的偏好会污染 B 公司；记忆闭环（阶段 18）依赖这个隔离
- 验收：两个组织并发出图，会话/记忆/统计互不可见；平台审计日志能对到智能体侧日志（同一 requestId）

### 波段 B · 稳定到可承诺 98%（阶段 5-8，约 1.5 周）——"不出错是测出来的"

**阶段 5 · 可靠性基线**
- 做法：幂等键（同一 agentRunId 重复提交不重跑）、任务级超时分层（文本 90s/出图 15min）、失败自动重试带退避、死信任务一键重放；生图/LLM 双 Key 池与配额熔断
- 验收：kill -9 智能体进程后重启，进行中任务可恢复或干净失败，无僵尸任务

**阶段 6 · 压测与容量**
- 做法：k6 压平台侧 + locust 压智能体侧；测出单实例并发出图上限、文本任务 QPS；超载时返回 429 + 平台队列削峰；给出扩容公式（N 并发 ≈ M 实例）
- 验收：产出容量报告；超载场景平台侧优雅排队而非雪崩

**阶段 7 · 质量评分体系可信化**
- 做法：一致性分覆盖全部任务类型（文本任务加 LLM-as-judge 评分：listing 合规分/关键词相关性分）；用 50 个人工标注样本校准阈值（现在 60 分阈值是拍脑袋的）
- 验收：评分与人工判断一致率 ≥85%；审核队列里的"该审的"确实是差的

**阶段 8 · SLO 98% 用数据说话**
- 做法：定义成功率 SLI（任务完成且质量分≥阈值 / 总任务）；Prometheus 面板 + 错误预算告警（2%/月烧完即冻结变更）；故障注入演练（断 LLM、断生图 API、断网、磁盘满四个场景）
- 验收：连续两周真实流量成功率 ≥98%，四个故障场景全部优雅降级——**从这天起你才有资格说"98% 不出错"**

### 波段 C · 聪明到懂你的平台（阶段 9-12，约 2 周）——"不只是执行，还会思考"

**阶段 9 · 平台数据回灌知识库**
- 做法：把平台里的真实业务数据（历史 listing、研究报告、关键词报表、审核通过/驳回记录）定期同步进智能体 knowledge_base，按 org 隔离
- 为什么：它现在只有通用行业知识，不知道**你的**类目、**你的**爆款、**你的**审核口味
- 验收：生成 listing 时能引用本组织历史高分案例的写法

**阶段 10 · 工具能力接平台真通道**
- 做法：机会雷达/竞品监控从"自带 SERPER Key 自己搜"升级为可调平台渠道数据（店铺快照、销量、库存）；封装成智能体内部工具集（tools registry）
- 验收：竞品周报里出现平台渠道的真实数据字段

**阶段 11 · 任务规划器（Planner）**
- 做法：新增 `plan_and_execute` taskType：接一句话目标（"给这个新品做全套上架准备"），LLM 拆成 DAG（研究→关键词→listing→出图→利润测算），逐步调用已有 taskType，中间结果传递
- 验收：一句话触发 5 步链式任务，任何一步失败能局部重试而非全盘重来

**阶段 12 · 自检器（Verifier）**
- 做法：每步执行后跑独立校验（listing 字数/违禁词/平台规则、图片一致性分、关键词去重）；不合格自动重做一次，再不合格标记给人审并说明原因
- 验收：故意注入劣质输出，系统能拦住并给出可读的失败原因——**这是 98% 里"错了能自己发现"的那部分**

### 波段 D · 主动自治（阶段 13-17，约 2.5 周）——"它来告诉你该干什么，然后自己干"

**阶段 13 · 平台事件感知**
- 做法：平台侧发事件（新品创建、库存低于阈值、差评告警、竞品换图、关键词排名跌）→ 消息队列 → 智能体订阅；智能体维护"感知收件箱"
- 验收：平台创建新品 30 秒内，智能体感知到并记录"待评估事项"

**阶段 14 · 主动建议（告诉你现在该做什么）**
- 做法：感知事项经机会评分 + 业务规则打分，生成结构化建议（"新品 X 还没有 listing，建议今天生成，预计收益/耗时/风险"），走平台通知系统推给对应角色
- 验收：不需要任何人开口，通知中心出现智能体的主动建议卡，附"一键执行"按钮

**阶段 15 · 自动排程（自动帮你安排工作）**
- 做法：建议被采纳（或命中"自动采纳规则"）后，智能体自动创建 TeamTask / AutomationFlow，排优先级和执行时间（避开配额高峰），登记到自己的工作日程表
- 验收：早上打开平台，任务板上已有智能体夜间安排好的当日工作队列

**阶段 16 · 授权分级与护栏**（**自治的安全阀，没有它阶段 17 不许上线**）
- 做法：操作分四级——只读分析（自动）/ 生成草稿（自动）/ 改动业务数据（低风险自动+事后报告）/ 对外发布与花钱（必须人工点确认）；org 级开关 + FeatureFlags 灰度；全部动作进审计日志；一键"暂停智能体"熔断按钮
- 验收：智能体永远无法未经确认发布 listing 或触发付费操作；每个自动动作都可追溯、可回滚

**阶段 17 · 全功能平台代理（自动处理平台上的所有功能）**
- 做法：给智能体发放受限 service token，封装平台全部业务 API 为工具（产品/listing/关键词/利润/任务/通知/文件）；每个工具带干跑（dry-run）模式；按阶段 16 分级放权
- 验收：一条指令"把这批 20 个新品全部完成上架准备"，智能体自动串完研究→文案→出图→利润→建任务→通知审核，全程无人介入（发布环节停在待确认）

### 波段 E · 记住并越干越好（阶段 18-20，约 1.5 周）——"完事不忘，下次更快"

**阶段 18 · 工作记忆（不能忘）**
- 做法：在 memory_store 上加"工作记忆层"：每个任务完结写入结构化记录（做了什么/为谁/结果/评分/人审结论/耗时），按 org 命名空间持久化到数据库（不再只是 JSON 文件）；提供"它干过什么"查询界面
- 验收：问"上周给产品 X 做过什么"，智能体准确列出全部工作与结果；重启/换机不丢

**阶段 19 · 复盘学习（越干越好）**
- 做法：人审驳回原因、低分案例自动归因进经验卡；下次同类任务规划时优先检索本组织经验卡（"这个类目上次白底图被驳回是因为阴影太重"）；每周自动生成"我这周干了什么、成功率、学到什么"报告
- 验收：同一类任务连续执行 10 次，后 5 次平均质量分显著高于前 5 次

**阶段 20 · 自治闭环验收（能直接干完活）**
- 做法：两周真实试运行——感知→建议→排程→执行→自检→汇报→记忆全链路开启（发布类动作保持人工确认）；每日记录成功率/干预率/建议采纳率
- 验收标准（同时满足才算"完美适配 + 自治上岗"）：
  1. 任务成功率 ≥98%（阶段 8 的 SLI 口径）
  2. 主动建议采纳率 ≥50%（说明建议真的有用而不是骚扰）
  3. 无人介入完成率 ≥80%（除发布确认外）
  4. 记忆问答准确率 100%（问什么答什么，不编造）
  5. 零越权操作（审计日志复核）

---

## 三、执行顺序与工期

| 冲刺 | 内容 | 工期 | 里程碑 |
|------|------|------|--------|
| 冲刺 1 | 阶段 1-4（适配） | 1 周 | 前端全程可视化 + 租户隔离，"被动工作"完全体 |
| 冲刺 2 | 阶段 5-8（稳定） | 1.5 周 | 拿到 98% 的**实测**证据 |
| 冲刺 3 | 阶段 9-12（聪明） | 2 周 | 一句话多步任务 + 自检拦截 |
| 冲刺 4 | 阶段 13-17（主动） | 2.5 周 | 主动建议→自动排程→全功能代理（带护栏） |
| 冲刺 5 | 阶段 18-20（记忆） | 1.5 周 | 两周试运行验收，正式"入职" |

**总计约 8.5 周。** 硬依赖：LLM API Key（文本+规划+评分）、生图 API Key、一台常驻运行智能体的服务器（现在是本机开发模式）、平台第四批"真验证"完成（数据库跑通是阶段 4/9/18 的前提）。

## 四、风险与取舍（提前说清楚）

1. **98% 是"成功率 SLO"不是"永不犯错"**：剩下 2% 靠自检拦截 + 人审兜底 + 死信重放消化，这是行业正确做法；承诺 100% 的都是骗子。
2. **主动性与打扰是天平**：阶段 14 必须带建议频控和"免打扰规则"，否则会变成垃圾通知制造机。
3. **自治必须先有护栏**：阶段 16 在 17 之前是刻意设计，顺序不可调换。
4. **LLM 成本**：主动模式下调用量会数倍增长，阶段 15 的排程器要带预算意识（每 org 每日 token 预算）。
