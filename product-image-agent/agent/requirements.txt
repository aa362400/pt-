# ============================================================
# Product Image Agent dependencies
# ============================================================
# Install:
#   pip install -r requirements.txt

# Web UI
flask>=3.0

# Production WSGI server (app.py auto-uses it when installed)
waitress>=3.0

# Image processing
Pillow>=10.0

# Multi-engine config
PyYAML>=6.0

# HTTP requests
requests>=2.28

# Shared task/session/idempotency state for multi-replica deployments
redis>=5.0

# Web page parsing for research
beautifulsoup4>=4.12

# Optional: JS page rendering for Taobao, Amazon, etc.
# playwright>=1.40
# After installing, run:
#   playwright install chromium

# Environment variable loading for CLI calibration and local execution
python-dotenv>=1.0

# Optional: AI subject cutout for subject_lock.py (best quality)
# Without it, subject_lock falls back to background-diff matting.
# rembg>=2.0
