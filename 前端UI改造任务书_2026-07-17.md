# 任务书：前端 UI 改造（导航重组 + 客户旅程向导化）

> 日期：2026-07-17
> 交付对象：编码智能体（Codex）
> 范围：`g:\平台\智能体前端`（Vite + React 19 + Tailwind 4 + react-router 7 + i18next）
> 配套文档：《选品到上架修复任务书_2026-07-17.md》（下称"修复任务书"）。本任务书多处承接其 P0-2 / P0-3 / P1-1 的前端部分，两份文档同时生效时以本文档的 UI 细节为准。

---

## 一、背景与目标

现状问题（已核实）：

1. 侧边栏 **25 个主菜单项平铺**（`src/lib/navigation.ts`），无分组、无优先级，客户找不到入口；选品相关入口就有 4 个（每日精准选品 / 商品调研 / Ozon 公开选品 / 趋势洞察），核价入口有 2 个。
2. 首页 `/assistant`（`pages/Dashboard.tsx`）是指标仪表盘，不回答"我今天该干嘛、我的货到哪一步了"。当前库里 92 条审核任务 PENDING 无人处理，直接原因就是没有"待你处理"入口。
3. `pages/` 下有 4 个已被 V2 取代的死代码页面。
4. "功能操作中心"（`pages/CapabilityCenter.tsx`，路由 `/operations-center`）以"前端/后端/智能体 已贯通"的系统视角展示状态，红色警告（如"需要配置 Ozon Performance client id/secret"）不带任何跳转和修复动作。
5. 失败信息大量直接透出英文异常。

目标：**把前端从"功能九宫格"改成"客户旅程导航"——登录即见待办与商品流水线，菜单按旅程分组，失败可读可重试。**

---

## 二、前端架构规则（必须遵守，改错层白干）

本项目是三层 UI 架构：

| 层 | 目录 | 职责 | 本次是否可改 |
|---|---|---|---|
| 展示层 | `src/figma-exact/` | Figma 高保真纯 UI 组件，只吃 props | **不改**（12 个组件全部保留） |
| 集成壳 | `src/pages-v2/` | 路由实际挂载页，拉数据 + 映射 + 注入 figma-exact | 可改 |
| 功能引擎 | `src/pages/` | 完整业务实现，部分被 V2 嵌入 | 可改被路由直接使用的；被嵌入的只包装不重写 |

固定改动点：路由 `src/AppRouter.tsx`；菜单 `src/lib/navigation.ts` + `src/components/sidebar/Sidebar.tsx`；状态→中文映射 `src/utils/*-presentation.ts`（新增映射必须带测试，放 `test/`）；文案进 `src/i18n/locales/zh-CN.json` 与 `en-US.json`。

硬性约束：

1. **禁止展示无证据数据**：任何价格/利润/指标若后端未提供证据，显示"证据不足，未核验"，不许显示 0、占位数或估算值。现有测试会守这条。
2. **面向用户文案全中文**，错误经 `src/utils/customer-facing-language.ts` 映射，不透英文堆栈。
3. **契约测试同步**：改导航/侧边栏/顶栏会命中 `test/topbar-navigation.test.ts`、`test/sidebar-logout-contract.test.ts`；删 `ReviewCenter.tsx` 会命中 `test/coderabbit-frontend-regressions.test.ts:180`。这些测试要**同步更新使其继续守住原意图**，不许删测试或跳过。
4. 验收门禁：`cd 智能体前端 && npm test && npx oxlint src && npm run build` 全绿。
5. 不引入新 UI 库/状态库，沿用 Tailwind + Context + useReducer 模式。

---

## 三、文件处置清单（先执行，作为任务 0）

### 3.1 删除（死代码，已验证无路由无 import）

| 文件 | 取代者 | 附带动作 |
|---|---|---|
| `src/pages/Automation.tsx` | `pages-v2/AutomationFlowV2.tsx` | 无 |
| `src/pages/StoreMonitor.tsx` | `pages-v2/PlatformConnectionV2.tsx` | 无 |
| `src/pages/ProductManagement.tsx` | `pages-v2/ProductManagementV2.tsx` | 无 |
| `src/pages/ReviewCenter.tsx` | `pages-v2/ApprovalCenterV2.tsx` | 更新 `test/coderabbit-frontend-regressions.test.ts` 中对该文件的静态引用（第 180 行附近），把断言对象改为 `ApprovalCenterV2.tsx` 中的等价逻辑，保持原检查意图 |

注意：`src/components/review/ProductResearchLaunchPanel.tsx`、`ManualPricingReviewForm.tsx` 同时被 V2 使用，**保留**。

### 3.2 保持不动

- `src/figma-exact/` 全部；`src/pages/ImageWorkbench.tsx` 与 `src/pages/ListingGenerator.tsx`（被 V2 嵌入的引擎，只允许加 props 包装）
- `src/api/`、`src/state/`、`src/auth/`、`src/hooks/`、`src/i18n/`（除新增文案 key）
- `Login`、`NotFound`、`BillingPage`、`OrderManagementV2`、`TeamSettingsV2`、`CustomerServiceV2` 及其占位页逻辑

---

## 四、任务列表

### 任务 UI-1：导航分组重构

**改动文件**：`src/lib/navigation.ts`、`src/components/sidebar/Sidebar.tsx`、`src/AppRouter.tsx`，相关测试。

**要求**：

1. `navigation.ts` 数据结构升级为分组制（保留现有 `routeTitleForPath` / `searchNavigation` 的对外行为与签名，内部适配）：

```
工作台        /workbench（新，任务 UI-2）、/assistant（运营总览，降为组内第二项）
选品          /daily-product-research（主入口，页内 Tab 整合 /product-research、/ozon-observations、/trend-radar）
上架与审核    /review、/listing-generator、/image-prompt、/ozon-pricing（合并利润计算）
店铺运营      /products、/orders、/marketing、/customer-service、/automation、/market、/competition、/supply-chain
设置与管理    /store-monitor、/team、/billing、/operations-center、/mcp-tools、/audit-logs、/agent-quality、/enterprise-readiness、/memory-governance、/enterprise-team、/agent-console、/agent-roadmap
```

2. 「设置与管理」组默认折叠；侧边栏支持分组折叠/展开，记忆状态到 `localStorage`。
3. 旧路由**一律保留可访问**（本任务只重组菜单，不删路由）；`/product-research`、`/ozon-observations`、`/trend-radar`、`/profit-calculator` 从菜单移除但路由保留，并在页面顶部加一条提示条「该功能已并入 XX，点击前往」。
4. 菜单合并只做**入口层面**：本任务不要求把 4 个选品页真的合成一页（那是后续迭代），只要求菜单只留 1 个主入口 + 页面内跳转链接。

**验收**：

- 侧边栏一级可见项 ≤ 5 组 + 组内合计 ≤ 16 项（当前 25 项）。
- `test/topbar-navigation.test.ts`、`test/sidebar-logout-contract.test.ts` 更新后通过；`searchNavigation` 仍能按标签搜到全部页面（含被移出菜单的旧页）。
- 手工验收：登录后每个菜单项可点、标题正确、无 404。

### 任务 UI-2：工作台首页（流水线待办）

**依赖**：修复任务书 P1-1 的后端端点 `GET /api/v1/dashboard/pipeline`。若该端点未就绪，本任务先用 `review.ts` + `dailyProductResearch.ts` + `listings.ts` 现有 API 在前端聚合出简化版（标注 TODO），端点就绪后切换。

**改动文件**：新增 `src/pages-v2/WorkbenchV2.tsx`（路由 `/workbench`，设为登录后默认跳转，替代 `/` → `/assistant`）；新增 `src/utils/pipeline-presentation.ts` + `test/pipeline-presentation.test.ts`；新增 `src/api/pipeline.ts`。

**要求**：

1. 首屏三块，从上到下：
   - **待你处理（N）**：按紧急度排序的动作列表，每条 = 一句中文描述 + 一个动作按钮（「去审核」「去定价」「重试」），点击直达对应页面并选中对应条目（用 query 参数，如 `/review?task=xxx`，目标页需支持读取该参数定位）。
   - **商品流水线**：横向阶段条「选品 → 审核 → 图片 → 刊登 → 发布」，每阶段显示在途数量，点击展开该阶段条目列表；失败条目红色，显示 `customer-facing-language` 映射后的中文原因 + 重试按钮。
   - **系统状态一行**：复用修复任务书 P0-1 的通道健康数据，仅在有通道 down 时显示红色横幅「图片生成通道不可用，将导致上架失败：查看详情」。
2. 阶段/动作的展示映射全部放 `pipeline-presentation.ts` 纯函数，禁止在组件里写死中文。
3. `/assistant`（Dashboard）保留原样，作为「运营总览」。

**验收**：

- 用现库数据渲染：待处理数 ≥ 90、失败可重试项可见且按钮可点。
- `pipeline-presentation.test.ts` 覆盖 ≥ 6 种状态组合的映射（研究中/待审核/审核通过待上架/图片失败/待发布审批/已发布）。
- 登录后默认落在 `/workbench`。

### 任务 UI-3：审批中心改造（`pages-v2/ApprovalCenterV2.tsx`）

**要求**：

1. 首屏第一区块固定为「待你处理」（复用 `src/utils/approval-center-workspace.ts` 的 actionable 队列），每条带明确动作按钮；processed 折叠到底部。
2. 支持 `?task=<id>` 查询参数直接定位并展开指定任务（配合 UI-2 跳转）。
3. 失败/驳回条目显示中文原因（errorCode 映射）+ 重试按钮（复用 `POST /agent-runs/:id/retry` 等现有端点，幂等）。
4. 人工定价表单（`ManualPricingReviewForm`）不改逻辑，只确保从待办点进来时表单自动聚焦。

**验收**：手工路径「工作台点『去定价』→ 审批中心自动定位该任务 → 完成定价」全程 ≤ 3 次点击；`test/approval-center-workspace.test.ts`、`test/approval-center-state.test.ts` 通过（如调整分组逻辑需同步更新）。

### 任务 UI-4：选品页状态可读化（`pages/DailyProductResearch.tsx`）

承接修复任务书 P0-2/P0-3 的前端部分：

1. 候选卡新增「证据不足」黄色状态（errorCode = `EVIDENCE_INSUFFICIENT`）：文案「仅找到 N/2 条 Ozon 公开来源，无法核验价格」+ 重试按钮；价格区显示「证据不足，未核验」。
2. 所有 FAILED run/候选显示映射后的中文原因；未知 errorCode 显示通用文案 + 原始 code。
3. 发起选品改为轻量三步向导（选关键词/类目 → 确认参数 → 发起），可以是同页分步表单，不强制新组件。
4. 页面顶部提供到 `/product-research`、`/ozon-observations`、`/trend-radar` 的 Tab 式跳转链接（配合 UI-1 的菜单合并）。

**验收**：`test/daily-product-research-*.test.ts` 全部通过（新增状态需扩展对应测试）；mock 一条 `EVIDENCE_INSUFFICIENT` 候选能正确渲染黄色状态与重试按钮。

### 任务 UI-5：功能操作中心去工程化（`pages/CapabilityCenter.tsx`）

1. 删除卡片内「前端/后端/智能体」三列贯通状态，替换为单一状态徽章：`可用 / 需要配置 / 依赖故障 / 待接入`（中文）。
2. 每条红色警告改造为**可行动待办**：警告文案 + 「去处理」按钮，跳转到能解决它的页面（凭证类 → `/store-monitor`；通道类 → 系统状态详情；订阅类 → 外链或说明弹窗）。跳转映射写成纯函数放 `src/utils/capability-actions.ts` + 测试。
3. 该页在 UI-1 中已移入「设置与管理」组，页面标题下加一句说明「此页面用于系统诊断与功能接入管理」。

**验收**：页面不再出现「贯通」字样；每条警告都有可点的处理入口；新增 `test/capability-actions.test.ts` 覆盖全部警告类型的跳转映射。

### 任务 UI-6：上架分步向导（`pages-v2/ListingOverviewV2.tsx`）

1. 从审核通过的候选进入上架时，右侧（或顶部）显示固定步骤条：**① 经济学确认 → ② 图片生成 → ③ 内容确认 → ④ 发布授权**，当前步骤高亮，已完成打勾，失败步骤红色 + 原因 + 重试。
2. 步骤状态从 `product_launches` 现有字段推导（status / failureCode / imageGenerationApproved / publishApprovedAt / publishExecutionGrant*，见后端 schema），推导逻辑放 `src/utils/launch-steps-presentation.ts` + 测试。
3. `ListingGenerator.tsx` 引擎不重写，作为步骤 ③ 的内容区嵌入。

**验收**：`test/launch-steps-presentation.test.ts` 覆盖至少 5 种 launch 状态到步骤条的映射；现库唯一一条 FAILED launch（图片 401）应显示为步骤 ② 红色 + 中文原因「图片生成通道 API Key 无效」。

---

## 五、执行顺序与提交要求

1. 顺序：**任务 0（删除）→ UI-1 → UI-5 → UI-3 → UI-4 → UI-2 → UI-6**。
   - UI-5/UI-3/UI-4 无后端依赖可先行；UI-2 依赖 P1-1 端点（或先做前端聚合的简化版）；UI-6 最后。
2. 每个任务独立 commit，提交信息中文（`feat:` / `fix:` / `refactor:` 前缀）。
3. 每个任务完成后跑：`npm test`、`npx oxlint src`、`npm run build`，结果贴入下方实施记录。
4. 涉及文案一律双语进 `zh-CN.json` / `en-US.json`；组件内不许硬编码新增中文（存量硬编码不要求本次清理）。
5. 禁止：删除/跳过既有测试；改 `figma-exact/`；引入新依赖；改动 `src/api/client.ts` 的鉴权与幂等逻辑。

## 六、实施记录（由执行者填写）

实施日期：2026-07-17。执行分支：`codex/coderabbit-review-20260716`。

| 顺序 | 任务 | 提交 | 主要交付 | 验证结果 |
|---|---|---|---|---|
| 0 | 删除旧页面 | `15e4b74` | 删除 4 个已被 V2 替代且无路由引用的页面，并迁移等价回归断言 | 前端完整测试、oxlint、生产构建通过 |
| 1 | UI-1 导航分组 | `289adb2` | 五组客户旅程导航、设置组默认折叠、折叠状态记忆、旧路由保留并提供合并提示 | 前端完整测试、oxlint、生产构建通过；浏览器确认 `/workbench` 无 404 |
| 2 | UI-5 功能操作中心 | `59ef13f` | 单一客户状态徽章、故障处理动作映射及测试、诊断说明 | 前端完整测试、oxlint、生产构建通过 |
| 3 | UI-3 审批中心 | `51249a1` | 待办优先工作区、`?task=` 精确定位、人工核价聚焦、失败重试与已处理折叠 | 182 项前端测试、oxlint、生产构建通过；真实页面显示待办 18、失败与重试 50、已处理 3 |
| 4 | UI-4 每日选品 | `fed1c70` | 三步真实选品向导、证据不足状态、失败重试、顶部旧功能 Tab、种子词传入后端运行快照 | 185 项前端测试、oxlint、生产构建通过；后端 DTO 测试及构建通过；真实候选和图片证据可见 |
| 5 | UI-2 工作台 | `1145601` | 默认 `/workbench`、真实流水线和通道健康、待办动作、五阶段展开及深链 | 191 项前端测试、oxlint、生产构建通过；真实流水线显示审核 116、图片 1、待处理 117 |
| 6 | UI-6 上架向导 | `7bfd8a7` | 产品发布详情接口、四步上架状态推导、发布记录深链、步骤三复用 `ListingGenerator` | 194 项前端测试、oxlint、生产构建通过；后端产品发布服务 19 项测试及构建通过 |
| 回归修复 | 数量插值 | `67b7cd7` | 按项目自定义 i18n 单花括号规则修复待办/失败/已处理数量 | 194 项前端测试、oxlint、生产构建通过；浏览器显示“待你处理（117）” |
| 回归修复 | 历史图片 401 | `dc0a910` | 根据真实 `failureMessage` 将通用 `PRODUCT_LAUNCH_FAILED` 识别为图片密钥故障，避免泄露原始英文错误 | 194 项前端测试、oxlint、生产构建通过；真实发布记录第 2 步显示“图片生成通道 API Key 无效” |

运行验收证据：

- 前端与后端 Docker 容器均完成重建；`GET /api/v1/health` 返回 `status: ok`，后端日志确认 `GET /api/v1/review/product-launch/:launchId` 已注册。
- 后端启动前生成数据库备份：`outputs/backups/shopmate-before-ui6-20260717-173424.dump`（2,123,160 字节）；启动日志显示 109 个迁移均已存在、无待执行迁移。
- 真实失败发布记录 `cmrofi6dd00eroc0157s7i59g`：经济学确认已完成；图片生成执行失败并显示中文原因；内容确认、发布授权等待前置步骤。
- 点击“重试失败步骤”后进入 `/review?task=cmroclye000q1py01s9x4y7jy`，审批详情自动打开，参考图上传、本地生成确认和“不向 Ozon 发布”边界可见。
- 浏览器控制台错误为 0；`src/figma-exact/` 未纳入本任务提交。
