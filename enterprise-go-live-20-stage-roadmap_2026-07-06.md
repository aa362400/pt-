# ShopMate AI 平台 · 企业级上线 20 阶段路线图

> 生成时间：2026-07-06
> 范围：`后端`（NestJS 11 + Prisma + PostgreSQL）、`智能体前端`（Vite + React）、`电商设计图保持产品一致性智能体`（Python Flask 双智能体）
> 现状基线：安全加固与三端链路打通已完成（Python 469 测试通过、前后端构建通过）；后端 24 个业务模块仍为 501 占位；前端多数页面仍用本地 mock 数据。

---

## 总览

| 阶段 | 主题 | 所属层 | 上线阻塞级别 | 状态 |
|---|---|---|---|---|
| 1 | 密钥与配置治理 | 基础设施 | P0 | 已完成 |
| 2 | 生产数据库与迁移/备份体系 | 基础设施 | P0 | 已完成 |
| 3 | 账号体系补全（验证/找回/2FA） | 后端 | P0 | 进行中 |
| 4 | 多租户与 RBAC 细化验证 | 后端 | P0 | 进行中 |
| 5 | 核心资源模块真实实现（一批） | 后端 | P0 | 已完成 |
| 6 | 电商业务模块真实实现（二批） | 后端 | P1 | 已完成 |
| 7 | 协作与运营模块真实实现（三批） | 后端 | P1 | 待开始 |
| 8 | 对象存储与文件安全 | 后端 | P0 | 待开始 |
| 9 | 智能体生产化（队列/并发/重试） | 智能体 | P0 | 待开始 |
| 10 | 生成质量与人审工作流 | 智能体 | P1 | 待开始 |
| 11 | 前端全量去 mock、接真实 API | 前端 | P0 | 待开始 |
| 12 | 前端体验完善与国际化 | 前端 | P2 | 待开始 |
| 13 | 可观测性（日志/指标/追踪/告警） | 基础设施 | P0 | 待开始 |
| 14 | 安全加固与渗透测试 | 全栈 | P0 | 待开始 |
| 15 | 性能压测与容量规划 | 全栈 | P1 | 待开始 |
| 16 | CI/CD 三环境流水线 | 基础设施 | P0 | 待开始 |
| 17 | 容器化与编排部署 | 基础设施 | P1 | 待开始 |
| 18 | 合规与数据治理 | 全栈 | P1 | 待开始 |
| 19 | 计费与商业化闭环 | 后端+前端 | P1 | 待开始 |
| 20 | 灰度上线与运维体系 | 运营 | P0 | 待开始 |

---

## 阶段明细

### 阶段 1 · 密钥与配置治理
- **状态**：已完成
- **目标**：任何密钥不进仓库，生产/预发/开发配置彻底分离。
- **关键任务**：清点全部 `.env`（后端、前端、agent）；引入密钥管理（云 KMS / Vault / 至少 CI Secret）；轮换历史上出现在 git 里的所有密钥；`.env.example` 与真实变量对齐。
- **验收**：git 历史扫描（gitleaks）零命中；生产启动仅依赖注入的环境变量。

### 阶段 2 · 生产数据库与迁移/备份体系
- **状态**：进行中
- **目标**：PostgreSQL 生产实例可用，迁移可重复、可回滚，数据可恢复。
- **已确认现状**：后端已接入 Prisma `schema.prisma` 与 `PrismaService`，开发环境通过 `docker-compose.yml` 提供 PostgreSQL + Redis，`HealthController` 已具备数据库/Redis 探活；当前缺口是生产级数据库部署、迁移执行、备份与恢复演练脚本。
- **关键任务**：托管 PG（含只读副本规划）；`prisma migrate deploy` 纳入发布流程；每日自动备份 + 恢复演练；连接池（pgbouncer/Prisma pool）参数定型。
- **已落地**：新增 `pnpm db:migrate:deploy` / `pnpm db:migrate:dev` / `pnpm db:backup` / `pnpm db:restore`；补齐 `scripts/db-backup.sh`、`scripts/db-restore.sh`、`scripts/db-refresh.sh`；` .env.example` 增加发布与演练提示。
- **验收**：一次完整的「备份 → 删库 → 恢复」演练成功；迁移在预发环境全量跑通。

### 阶段 3 · 账号体系补全
- **状态**：进行中
- **目标**：注册/登录达到企业标准。
- **已落地**：`auth` 接口新增 `forgot-password` / `reset-password`；注册后创建组织与成员关系；刷新 token 旋转与吊销保持不变；`User` 新增 `emailVerifiedAt` / `twoFactorEnabled` / `twoFactorSecret`，并新增 `PasswordResetToken`。
- **关键任务**：邮箱验证、找回密码（带过期 token）；可选 TOTP 两步验证；登录异常通知；会话管理页（吊销单设备）。
- **验收**：全流程 e2e 测试；密码策略与锁定策略生效。

### 阶段 4 · 多租户与 RBAC 细化验证
- **状态**：进行中
- **目标**：跨组织数据零泄漏。
- **已落地**：`organizations` 服务已加上 `OWNER/ADMIN` 与 `OWNER` 级别的组织操作权限校验；`org-scope.ts` 新增 `requireOrgRole()`，统一做组织级角色判断。
- **关键任务**：为每个查询补 orgId 过滤的集成测试（租户 A 造数、租户 B 访问必须 404/403）；OWNER/ADMIN/MEMBER 权限矩阵文档化并测试；邀请成员/转让所有权流程。
- **验收**：租户隔离测试套件 100% 通过并入 CI。

### 阶段 5 · 核心资源模块真实实现（第一批）
- **状态**：已完成
- **范围**：`users`、`organizations`、`workspaces`、`products`、`files`。
- **已落地**：`users` 支持个人资料与组织成员列表；`organizations` 支持组织详情、成员管理与角色调整；`products`、`files` 已具备真实 CRUD / 分页 / 搜索 / 组织隔离；`files` 使用对象存储抽象写入，避免本地磁盘单点。
- **关键任务**：按已有 Prisma 模型实现 CRUD + 分页 + 搜索；移除 501 占位；补 DTO 校验与服务层测试。
- **验收**：模块级集成测试通过；前端对应页面可真实增删改查。

### 阶段 6 · 电商业务模块真实实现（第二批）
- **范围**：`listings`、`keywords`、`product-research`、`trends`、`image-prompt`。
- **关键任务**：Listing 生成对接 LLM（复用 agent-runs 管道）；关键词/趋势先接静态数据源再演进第三方 API；结果落库可回看。
- **验收**：一条「选品 → 关键词 → Listing 草稿」的完整业务流可走通。

### 阶段 7 · 协作与运营模块真实实现（第三批）
- **范围**：`tasks`、`sops`、`notifications`、`audit-logs`、`dashboard`、`assistant`。
- **关键任务**：通知走已注册的 BullMQ worker 实际投递（站内 + 邮件）；审计日志在关键写操作处埋点；仪表盘聚合真实数据。
- **验收**：操作后可在审计日志与通知中心看到真实记录。

### 阶段 8 · 对象存储与文件安全
- **目标**：文件不落本地磁盘单点。
- **关键任务**：接入 S3/OSS/COS，上传走预签名 URL；类型/大小白名单、图片重编码防伪装；智能体产出图同步到对象存储并出 CDN 链接。
- **验收**：删除任一应用节点不丢文件；上传恶意文件被拒。

### 阶段 9 · 智能体生产化
- **目标**：生图任务在生产负载下稳定。
- **关键任务**：任务队列从文件持久化升级到 Redis/RQ（或保留现有队列但补并发上限与优先级）；失败重试与死信；生图 Key 池与配额监控；超时/部分失败的明确状态回传给平台。
- **验收**：并发 20 任务压测无丢单；kill 进程后任务可恢复或标记失败。

### 阶段 10 · 生成质量与人审工作流
- **目标**：交付给商家的图有质量兜底。
- **关键任务**：一致性分阈值策略（低于阈值自动重生成 N 次）；平台侧人审队列（通过/驳回/重做）；结果与用户反馈回流 memory_profile。
- **验收**：低分图不会直接标记 done；人审操作有审计。

### 阶段 11 · 前端全量去 mock
- **目标**：所有页面数据来自真实 API。
- **关键任务**：逐页替换 mockData（自动化流程、选品、关键词、Listing、Prompt 库、项目空间等）；空态/加载态/错误态三态齐全；删除残留 mock 导出。
- **验收**：`mockData.ts` 删除或仅剩演示开关；断网时页面有合理降级。

### 阶段 12 · 前端体验完善与国际化
- **关键任务**：i18n（中/英）；移动端响应式过一遍核心流程；键盘可达性与焦点管理；骨架屏统一。
- **验收**：Lighthouse a11y ≥ 90；核心流程在 375px 宽度可用。

### 阶段 13 · 可观测性
- **目标**：出问题 5 分钟内能定位。
- **关键任务**：结构化日志聚合（Loki/ELK/云日志）；指标（Prometheus + Grafana：QPS、P95、队列深度、生图成功率）；分布式追踪（requestId 已有，补 OpenTelemetry）；告警规则（错误率、队列积压、磁盘、证书）。
- **验收**：注入一次故障，告警在 5 分钟内触发且能通过 trace 定位。

### 阶段 14 · 安全加固与渗透测试
- **关键任务**：依赖漏洞扫描进 CI（pnpm audit / pip-audit）；OWASP Top10 自查（SSRF 已有 fetch_url 防护，复核）；WAF / 反爬 / IP 限流；第三方渗透测试并修复。
- **验收**：高危漏洞清零，渗透报告归档。

### 阶段 15 · 性能压测与容量规划
- **关键任务**：k6/Locust 压测核心接口（登录、agent-runs、列表页）；数据库慢查询治理与索引复核；Redis/HTTP 缓存策略；CDN 静态资源。
- **验收**：目标并发下 P95 < 500ms（非生图接口）；容量报告给出扩容水位线。

### 阶段 16 · CI/CD 三环境流水线
- **关键任务**：GitHub Actions（或等价）跑 lint + 测试 + 构建 + 迁移演练；dev/staging/prod 三环境；staging 自动部署、prod 手动审批；回滚脚本演练。
- **验收**：一次提交到 staging 全自动；prod 回滚 < 10 分钟。

### 阶段 17 · 容器化与编排部署
- **关键任务**：三个服务的生产 Dockerfile（agent 已有，补前后端多阶段构建）；docker-compose 生产版或 K8s（探针、资源限额、HPA）；Nginx/网关统一入口与 TLS。
- **验收**：单命令拉起全栈；滚动更新零停机。

### 阶段 18 · 合规与数据治理
- **关键任务**：隐私政策与用户协议；数据保留与删除策略（含智能体 session/生成图的 TTL，housekeeping 已有基础）；用户数据导出/注销；GDPR/个保法自查清单。
- **验收**：注销账号后数据在承诺期限内清除，可审计。

### 阶段 19 · 计费与商业化闭环
- **关键任务**：`billing` 模块真实实现：套餐/配额（生图张数、成员数）；对接支付（Stripe/微信/支付宝按目标市场）；用量计量（agent-runs 计数）与超额限制；发票与账单页。
- **验收**：从订阅 → 用量扣减 → 超额拦截 → 续费全流程可走通。

### 阶段 20 · 灰度上线与运维体系
- **关键任务**：灰度发布（按组织白名单）；SLO/SLA 定义（可用性 99.9%）；运行手册（Runbook：常见故障处置）；值班与升级机制；上线检查清单执行。
- **验收**：灰度两周错误率达标后全量；Runbook 演练一次。

---

## 建议执行顺序

P0 串成主线：**1 → 2 → 16 → 5 → 4 → 3 → 8 → 9 → 11 → 13 → 14 → 20**，
P1/P2（6、7、10、12、15、17、18、19）在主线各节点后并行插入。
最短可上线路径（MVP 企业版）≈ 主线 12 个阶段完成即可灰度。
