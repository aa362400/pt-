# 任务书：修复「选品 → 上架」全链路 + 失败可见性改造

> 日期：2026-07-17
> 交付对象：编码智能体（Codex）
> 工作区：`g:\平台`（monorepo：`后端/` NestJS + Prisma、`智能体前端/` Vite + React 19、`电商设计图保持产品一致性智能体/` Flask Python Agent）

---

## 一、背景与诊断结论（已核实，勿重复排查）

平台容器全部 healthy（`shopmate-local-*`），但业务数据表明主流程几乎完全失败：

| 数据（来自 `shopmate_codex` 库） | 数值 |
|---|---|
| `agent_runs` 失败率 | 111 / 127（87%），其中 PRODUCT_RESEARCHER 失败 68、PLANNER 失败 28、IMAGE_CREATIVE 失败 13 |
| `product_launches` | 历史仅 1 条，状态 FAILED（图片生成 401） |
| `review_tasks` 积压 | 92 条 PENDING、24 条 REWORK，无人处理 |

失败原因三类（`agent_runs.errorMessage` 聚合统计）：

1. **Ozon 证据门禁 fail-closed 直接判死**（约 67 条）：「未获取至少两条 Ozon 公开商品来源，未生成报告」「Ozon 公开商品来源中不足两条可解析的 RUB 价格」等。搜索通路（Serper/Tavily）拿不到证据 → 整个 run 直接 FAILED，无中间状态、无补救入口。
2. **LLM 通道故障**（约 35 条）：第三方代理 `jojocode.com` / `api2.jojocode.com` 返回 503 / 403 / 额度耗尽。
3. **图片 API Key 无效**（约 15 条）：OpenAI image 接口 401 `INVALID_API_KEY`，导致上架流程在图片生成阶段必死。

**前置条件（由人工完成，不在本任务书范围）**：更换 `电商设计图保持产品一致性智能体/agent/.env` 中的图片 Key 与 LLM 代理 Key。但本任务书的改造目标是：**即使 Key 坏了，用户也能在界面上第一时间看懂"哪个通道坏了、怎么修"，且流程不会无声死掉。**

---

## 二、全局约束（每个任务都必须遵守）

1. **禁止伪造数据**：平台核心原则是证据驱动 + fail-closed。任何改造不得让前端展示无证据支撑的价格/利润/关键词数据。「软化门禁」指引入可恢复的中间状态，不是放行假数据。
2. **契约三份镜像同步**：`contracts/agent-tasks.contract.json`、`后端/contracts/agent-tasks.contract.json`、`电商设计图保持产品一致性智能体/agent/contracts/agent-tasks.contract.json` 三份必须字节一致。若本次改动需要变更契约（尽量避免），必须三处同步并升 `contractVersion`，并保证 `后端/test/agent-contract.spec.ts` 与 Python 侧 `agent/tests/test_contract.py` 通过。
3. **迁移治理**：任何 Prisma schema 变更必须新建迁移目录（`migration.sql` + `rollback.sql` + `metadata.json` 三件套），登记进 `后端/prisma/migration-governance.json` 的当前 OPEN release，且通过 `后端/test/migration-governance.spec.ts`。禁止修改任何已冻结迁移。
4. **测试门禁**：后端 `npm run release:verify`（在 `后端/` 下）、前端 `npm test`（Node 原生 test runner）、Python `pytest`（在 `电商设计图保持产品一致性智能体/agent/` 下）必须全绿。新逻辑必须带测试。
5. **多租户**：所有新表/新字段照常带 `organizationId` 并纳入 RLS（参考现有迁移模式，验证脚本 `后端/src/cli/verify-rls.ts`）。
6. **面向用户的文案一律中文**，走前端 `智能体前端/src/utils/customer-facing-language.ts` 的映射机制，不得把英文异常堆栈直接透给用户。

---

## 三、任务列表

### 任务 P0-1：依赖通道自检（Preflight Health）

**目标**：LLM 通道、图片生成通道、Web 搜索通道的可用性要主动探测、持续暴露，坏了在前端首页和相关操作入口直接显示中文原因与修复指引。

**现状锚点**：
- Python 侧已有 LLM 探活雏形：`电商设计图保持产品一致性智能体/agent/web/services/llm_runtime.py`（可用性探测、降级状态），健康路由在 `agent/web/routes/core.py`（`/api/health`、`/api/ready`、`/api/metrics`）。
- 后端已有健康模块（`后端/src/` 下 Health 相关）与前端运维组件 `智能体前端/src/components/ops/SystemHealthOverview.tsx`。

**要求**：
1. Python Agent 新增（或扩展现有）`GET /api/v1/agent/health/channels`：返回三个通道的结构化状态，形如：
   ```json
   {
     "llm":    { "status": "ok|degraded|down", "provider": "...", "lastError": "...", "checkedAt": "..." },
     "image":  { "status": "down", "lastError": "OpenAI image API 401 INVALID_API_KEY", "checkedAt": "..." },
     "search": { "status": "ok", "provider": "serper", "checkedAt": "..." }
   }
   ```
   - 探测方式：轻量真实调用（如 LLM 1-token 补全、图片引擎 dry-run 或读取最近一次真实调用的失败缓存、搜索一次廉价查询），结果缓存 ≥5 分钟，避免烧额度。
   - 401/403/额度类错误必须被识别并归类（复用现有错误码 `MODEL_PROVIDER_QUOTA_EXHAUSTED`、`IMAGE_PROVIDER_FALLBACK_EXHAUSTED`、`INVALID_API_KEY` 的判断逻辑，散布在 `agent/scripts/generate_batch.py`、`agent/web/services/commerce_llm.py` 等处）。
2. 后端新增聚合端点（挂在现有 agent-console 或 health 模块下）：`GET /api/v1/agent-console/channel-health`，转发 Python 侧结果并附加后端自身对 Agent 服务的连通性判断。带组织鉴权，普通成员可读。
3. 前端：
   - `SystemHealthOverview.tsx` 增加「AI 通道」卡片：三个通道各一行，红/黄/绿 + 中文原因 + 修复指引（例如：「图片生成通道不可用：API Key 无效。请联系管理员在智能体配置中更换 IMAGE_API_KEY / OPENAI_IMAGE_API_KEY」）。
   - 在选品发起页（`智能体前端/src/pages/DailyProductResearch.tsx`）和上架确认入口（`智能体前端/src/pages-v2/ApprovalCenterV2.tsx` 的产品上架确认区）做**前置拦截提示**：当依赖通道 down 时，在提交按钮附近显示警告横幅（不禁用按钮，允许用户明知故犯地重试）。
4. 不改契约文件：该健康端点属于 transport 之外的运维接口，走 `X-Api-Key` 鉴权即可（与现有 `/api/v1/agent/health` 同级）。

**验收**：
- 人为设置一个坏的 IMAGE_API_KEY 后，前端 SystemHealthOverview 在 5 分钟内显示图片通道红色 + 中文原因。
- Python 单测覆盖：三种通道状态分类（ok/degraded/down）、错误归类（401→Key 无效、429/quota→额度、5xx→服务不可用）。
- 后端单测覆盖：聚合端点鉴权 + Agent 不可达时返回 `agent: down` 而非 500。

---

### 任务 P0-2：Ozon 证据门禁软化（引入「证据不足」可恢复状态）

**目标**：选品 run 在「搜索拿不到 ≥2 条 Ozon 公开来源」时，不再把整个 run 判成 FAILED，而是进入可识别、可重试、可人工补证据的中间状态。

**现状锚点**：
- 证据门禁逻辑在 Python 侧：`电商设计图保持产品一致性智能体/agent/web/services/research_evidence.py`、`platform_tasks.py`（product_research / trend_analysis 的 Ozon 证据绑定）、`global_product_discovery.py`。
- 后端编排：`后端/src/features/product-research/daily/services/daily-product-research-orchestrator.service.ts`（10 阶段流水线），run 状态里已有 `PARTIAL`。
- 失败文案「未获取至少两条 Ozon 公开商品来源」等即出自上述 Python 服务。

**要求**：
1. Python 侧：证据不足时不抛通用异常，改为返回结构化结果，`errorCode` 统一为 `EVIDENCE_INSUFFICIENT`，携带已拿到的部分证据（如只找到 1 条来源）与缺口说明（缺几条、试过哪些搜索 provider）。**不产出任何估算/编造的价格数据。**
2. 后端：
   - `AgentRun` 已有 `errorCode` 字段；worker（`后端/src/workers/agent-run.worker.ts`、`daily-product-research.worker.ts`）识别 `EVIDENCE_INSUFFICIENT`，将对应阶段标记为「证据不足」而非硬失败；run 整体走 `PARTIAL`（已存在该状态，当前库里有 27 条 PARTIAL 证明通路存在）。
   - `PARTIAL` 的 run 支持**针对性重试**：只重跑证据采集失败的候选/阶段，复用现有 checkpoint 机制（`checkpointStage/StepIndex`）。确认现有重试端点（前端 `智能体前端/src/api/dailyProductResearch.ts` 已有 retry 调用）能覆盖这种场景，不能则补。
3. 前端 `DailyProductResearch.tsx`：
   - 「证据不足」候选显示黄色状态 + 明确文案：「仅找到 1/2 条 Ozon 公开来源，无法核验价格。可点击重试或更换关键词」，附重试按钮。
   - 严禁把这类候选的价格区展示为 0 或占位数字——显示「证据不足，未核验」。
4. 若需要新增枚举值/字段：走迁移治理流程（见全局约束 3）。优先复用现有字段（`errorCode`、`progress` jsonb、`PARTIAL` 状态），尽量做到**零 schema 变更**。

**验收**：
- 模拟搜索 provider 全部失败（单测 mock），run 结束状态为 PARTIAL 而非 FAILED，候选带 `EVIDENCE_INSUFFICIENT`。
- 重试后（mock 搜索恢复）该候选能补全证据并继续走完评分阶段。
- Python 测试：`agent/tests/test_ozon_*`、`test_global_product_discovery.py` 相应扩展；后端测试：`daily-product-research-*.spec.ts` 扩展；全部通过。

---

### 任务 P0-3：失败原因人话化 + 一键重试全覆盖

**目标**：用户在任何页面看到的失败都是中文的、可行动的；能重试的失败都有重试按钮。

**现状锚点**：
- 前端错误映射：`智能体前端/src/utils/customer-facing-language.ts`（已有基础，覆盖不全）。
- Agent run 重试端点已存在：`POST /agent-runs/:id/retry`（见 `智能体前端/src/api/agentRuns.ts`）。
- 后端错误落库：`agent_runs.errorCode` / `errorMessage`。

**要求**：
1. 梳理本次诊断出现的全部真实错误形态，在后端或 Python 侧**归一化为稳定 errorCode**（至少覆盖）：
   - `MODEL_PROVIDER_UNAVAILABLE`（上游 5xx，如 jojocode 503）
   - `MODEL_PROVIDER_QUOTA_EXHAUSTED`（额度/403）
   - `IMAGE_PROVIDER_INVALID_KEY`（401 INVALID_API_KEY）
   - `EVIDENCE_INSUFFICIENT`（任务 P0-2）
   - `EVIDENCE_QUALITY_GATE_FAILED`（「Ozon 研究证据未通过结构化质量门禁」）
2. 前端 `customer-facing-language.ts` 为每个 errorCode 提供：中文标题、一句话原因、建议动作（「更换 API Key」「稍后重试」「补充证据后重试」）。未识别的 code 显示通用文案 + 原始 code（便于报障），**不显示英文堆栈**。
3. 失败的 agent run / 选品候选 / 上架任务在以下页面统一提供重试入口（幂等，复用现有 retry 端点）：`DailyProductResearch.tsx`、`ApprovalCenterV2.tsx`、`AgentConsole.tsx`（如存在运行列表）。
4. 历史存量：写一个一次性的后端脚本（放 `后端/scripts/`，不进迁移）把存量 `agent_runs.errorMessage` 按上述规则回填 `errorCode`，可选执行。

**验收**：
- 前端测试扩展 `智能体前端/test/customer-facing-chinese.test.ts`：每个新 errorCode 有中文映射且不含英文堆栈特征。
- 手工验证：把一条 FAILED run 的详情打开，能看到中文原因 + 重试按钮。

---

### 任务 P1-1：贯穿式流水线视图（「我的商品在哪一步」）

**目标**：新增一个页面（或改造现有审批中心首屏），把「选品运行 → 候选 → 审核 → 图片 → Listing → 发布」呈现为一条可视流水线，回答三个问题：现在到哪了、卡在哪、下一步需要我做什么。

**现状锚点**：
- 92 条 PENDING review 无人处理，说明用户找不到入口。
- 数据已齐：`review_tasks`（entityType: PRODUCT_RESEARCH / AGENT_RUN…）、`product_launches`（status + failureCode）、`product_research_runs`（status + checkpoint）。
- 前端已有工作队列分组逻辑：`智能体前端/src/utils/approval-center-workspace.ts`（actionable / needs_attention / processed 三队列）。
- 后端聚合可参考 `后端/src/features/dashboard/`。

**要求**：
1. 后端新增聚合端点 `GET /api/v1/dashboard/pipeline`（按组织）：返回每个「在途商品」的流水线位置：
   ```json
   {
     "items": [{
       "candidateId": "...", "title": "...",
       "stage": "RESEARCH|REVIEW|IMAGE|LISTING|PUBLISH_REVIEW|PUBLISHED|FAILED",
       "blockedOn": { "type": "USER_ACTION|SYSTEM_RETRY|CHANNEL_DOWN", "label": "等待人工定价审核", "link": "/review?task=..." },
       "errorCode": null
     }],
     "summary": { "awaitingUser": 92, "failedRetryable": 15, "inProgress": 3 }
   }
   ```
2. 前端：
   - 在 `ApprovalCenterV2.tsx` 顶部（或新页 `PipelineOverviewV2`，挂到导航 `智能体前端/src/lib/navigation.ts`）渲染流水线看板：横向阶段条 + 每阶段计数，点击进入过滤后的列表；每个条目显示「下一步动作」按钮直达对应操作页。
   - 首页 `Dashboard.tsx` 加一个「待你处理 N 件」的醒目入口，指向该视图。
3. 阶段推导逻辑放在后端 service 内并写单测；前端只做展示映射（放 `智能体前端/src/utils/pipeline-presentation.ts` + 对应测试）。

**验收**：
- 用现库数据（92 pending / 1 failed launch）能正确渲染：awaitingUser ≥ 90、failed 可重试项带重试按钮。
- 后端单测：阶段推导覆盖至少 6 种状态组合（研究中 / 待审核 / 审核通过待上架 / 图片失败 / 待发布审批 / 已发布）。

---

### 任务 P2（可选，时间允许再做）

1. **LLM 多 provider 降级链**：Python 侧 `llm_runtime.py` 支持按序 fallback（主代理 → 备用 Key → 备用模型），失败时向后端上报降级事件。
2. **死信重试面板**：前端暴露 `后端/src/features/dead-letter/` 的 triage/replay 能力。
3. **额度监控**：通道自检数据接入 Prometheus 指标（后端已有 `@willsoto/nestjs-prometheus`）。

---

## 四、执行顺序与提交要求

1. 顺序：P0-1 → P0-3 → P0-2 → P1-1（P0-1/P0-3 无 schema 风险，先落地见效；P0-2 涉及状态机语义，需要更谨慎）。
2. 每个任务独立提交（commit 粒度按任务），提交信息中文，格式参考现有 git log（如 `feat: ...` / `fix: ...`）。
3. 每个任务完成后必须跑对应测试套件并在最终汇报中贴结果：
   - 后端：`cd 后端 && npm run release:verify`
   - 前端：`cd 智能体前端 && npm test && npm run build`
   - Python：`cd 电商设计图保持产品一致性智能体/agent && python -m pytest`
4. 不得改动：已冻结迁移、契约文件（除非确有必要并三处同步）、`release/` 下的 RC1 工件。
5. 最终交付一份简短的变更清单（改了哪些文件、新增哪些端点、新增哪些 errorCode），追加到本文档末尾的「实施记录」章节。

## 五、实施记录（由执行者填写）

### 2026-07-17 实施结果

#### P0-1：依赖通道自检

- Python Agent 新增 `GET /api/v1/agent/health/channels`，并发执行 LLM、图片、搜索三个轻量真实探测；结果至少缓存 300 秒，响应会归一化 Key 无效、额度不足、服务不可用等错误，且不会返回密钥。
- 后端新增组织鉴权端点 `GET /api/v1/agent-console/channel-health`。Agent 不可达时返回结构化 `agentConnection: unavailable`，不会抛出 500。
- 前端新增中文 AI 通道状态卡，并在每日选品与审批中心显示操作前警告。警告不会伪装通道可用，也不会强制禁用用户重试。
- 真实运行结果：Agent 连通；LLM 为 `available`（`openai-compatible/deepseek-chat`）；搜索为 `available`（`serper`）；图片为 `unavailable`（`IMAGE_PROVIDER_UNAVAILABLE`）。同一缓存窗口内第二次请求复用相同 `checkedAt`，请求耗时约 11 ms。

#### P0-3：失败原因归一化与安全重试

- 后端新增稳定错误码归一化，覆盖 `MODEL_PROVIDER_UNAVAILABLE`、`MODEL_PROVIDER_QUOTA_EXHAUSTED`、`IMAGE_PROVIDER_INVALID_KEY`、`EVIDENCE_INSUFFICIENT`、`EVIDENCE_QUALITY_GATE_FAILED` 等真实错误。
- Worker 按错误码区分可重试与不可恢复失败；前端统一展示中文标题、原因和建议动作，不向客户透出英文堆栈。
- 审批中心失败项使用稳定幂等键调用真实 `POST /agent-runs/:id/retry`，成功后再更新当前任务状态，避免只改界面未重跑任务。
- 新增一次性回填命令 `npm run agent-errors:backfill`。真实数据库执行结果：扫描 37 条存量失败记录，符合回填条件 0 条，实际更新 0 条；未猜测或覆盖已有错误码。

#### P0-2：证据不足的部分成功与针对性重试

- Python 选品在只获取一个真实来源或来源不足时保留已验证证据，返回 `PARTIAL + EVIDENCE_INSUFFICIENT`、证据缺口及已尝试 provider；缺失价格保持 `null`，不填 0、不估算。
- 后端连接器、Provider 与编排器保留结构化部分结果；仅 `PARTIAL + EVIDENCE_INSUFFICIENT` 可从 `COLLECT` 阶段针对性重试，并复用现有证据和去重机制。
- 前端以黄色中文状态展示“仅找到 1/2 个独立来源”，价格显示“证据不足，未核验”，并提供真实重试入口。

#### P1-1：选品到上架流水线

- 后端新增组织隔离端点 `GET /api/v1/dashboard/pipeline`，统一聚合选品运行、审批任务和发布任务，后端负责推导 7 个阶段及下一步动作。
- 审批中心顶部新增完整中文流水线；首页新增“待你处理”指标。历史英文商品标题通过已审计的中文命名规则转换，未知英文不会直接暴露给客户。
- 真实管理员组织数据与页面一致：流水线共 80 项，其中选品 2、证据与核价 10、人工审批 67、商品资料与图片 1、发布快照 0、Ozon 上架 0、结果监控 0；待处理 80、阻塞 80、执行中 0、监控中 0。
- 浏览器验证截图：`outputs/browser-validation/review-pipeline-20260717.png`。审批中心同时真实显示待我处理 18、异常与重做 50、已处理 3，共 71 项。

#### 发布级验证与运行证据

- 后端 `npm run release:verify`：退出码 0；109 个迁移治理校验通过，Nest 构建、release lint、Prisma validate 与 release 测试全部通过。
- 前端 `npm run release:verify`：171/171 测试通过，TypeScript 与 Vite 生产构建通过。
- Python `python -m pytest -q`：956 项通过，另有 7 个子测试通过；33 条为 Pillow 未来弃用警告，无测试失败。
- 本地容器通过官方启动/检查脚本验证：PostgreSQL、Redis、Agent、后端、前端、Nginx 均 healthy，网关为 `0.0.0.0:80`。
- 数据库修改前备份：`.local-server/backups/pre-full-fix-20260717-1510.dump`（2,101,823 字节）和 `.local-server/backups/pre-full-fix-20260717-1510-globals.sql`（950 字节）。
- 本批修复未新增 Prisma schema 字段，未改任务契约，未修改冻结迁移。

#### 尚未解除的外部阻塞

- 图片生成通道仍真实不可用；需要管理员更换有效的图片服务配置后重新探测。系统现在会明确阻断并给出中文原因，不会生成占位图或伪造成功。
- 真实库中候选商品 211 个：`HOLD` 6 个、`REJECTED` 205 个；发布任务仅 1 条且为 `FAILED`；已提交或已在 Ozon 上架为 0。因此本次不能宣称已完成真实上架或连续 3 轮端到端验收。
- 每日自动选品当前为 `PAUSED`，未因本次“继续修复”而擅自恢复。收到明确 `/resume` 后才能继续领取新任务。
- P2 为可选项，本次未实施。

#### 提交前双轴审查补正

- 审查发现并修复：图片模型元数据可读不再等同“可生图”，状态改为“降级、待真实生图验证”；前端不再显示原始 provider 错误或英文异常；通用模型 HTTP 403 归类为额度不足。
- 流水线已从批次粒度改为候选商品粒度，返回 `candidateId`、结构化阻塞类型/中文说明/直达链接和 `failedRetryable` 计数。
- 容器重建后的真实审批页显示：候选级流水线共 117 项，证据与核价 49、人工审批 67、商品资料与图片 1，其余阶段 0；图片通道仍真实不可用。
- 双轴审查后全量门禁再次通过：后端 `release:verify` 退出码 0，前端最终 173/173，Python 956 项及 7 个子测试通过。
