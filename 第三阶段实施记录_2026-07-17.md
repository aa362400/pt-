# 体验优化与效率增强（第三阶段）实施记录

日期：2026-07-17
工作区：`G:\平台`

## E-0：端到端验收固化

状态：**硬阻塞（dry 门禁已完成；补充决议指定的人工定价路径无法推进到发布审批，未验收通过）**

### 已实施

- 新增 `后端/scripts/e2e-pipeline-smoke.mjs`。
- 支持 `--dry-channels`，明确标记 mock 通道，不调用真实 LLM、图片或 Ozon。
- 支持真实 HTTP 模式：登录、选择组织内 Ozon 工作区和真实 `PRODUCT_IMAGE`、创建每日选品、轮询终态、读取第一个候选、校验 `VERIFIED/PASS` 利润证明、获取审核任务、确认本地图片与 Listing 准备、轮询至 `AWAITING_PUBLISH_APPROVAL`。
- 真实模式没有实现也不会调用发布端点；证据固定记录 `externalPublishAttempted=false`。
- 每阶段记录开始时间、完成时间、耗时、状态、`errorCode` 与脱敏证据。
- 新增 dry 状态机测试与真实 HTTP 路由映射测试；路由测试断言请求日志不存在 `/publish`。
- `verify-platform-release.mjs` 已增加 `e2e-pipeline-smoke-dry` 组件，dry 证据单独写入 `.agent-runtime/e2e-pipeline-smoke-dry.json`。

### 已验证

- `node --check scripts/e2e-pipeline-smoke.mjs`：通过。
- `node --check test/fixtures/e2e-pipeline-smoke-server.mjs`：通过。
- `npx jest --config ./test/jest-e2e.json --runInBand test/e2e-pipeline-smoke.spec.ts`：2 个测试全部通过。
- `node verify-platform-release.mjs`：通过，退出码 0，总耗时 203.1 秒；后端构建、迁移治理、Prisma、全量测试、依赖审计、E2E dry smoke、前端发布验证与依赖审计、Agent Judge 回归、Python 全量回归和空白检查全部通过。
- dry 证据：`.agent-runtime/e2e-pipeline-smoke-dry.json`，状态 `PASSED`，停止点 `AWAITING_PUBLISH_APPROVAL`，未尝试外部发布。
- 真实证据：`.agent-runtime/e2e-pipeline-smoke.json`，状态 `FAILED`，错误码 `UNAUTHORIZED`；本地服务器配置中的种子密码调用真实 `/api/v1/auth/login` 返回 HTTP 401。

### 当前真实阻塞

1. 种子组织账号的当前密码与 `.env.local-server` 中 `SEED_ADMIN_PASSWORD` 不一致，真实登录返回 401。没有重置密码、没有读取浏览器会话、没有绕过认证。
2. Agent 实时健康状态显示供应商精确报价未配置：
   - `configured=false`
   - `enabled=false`
   - `exactQuote=false`
   - `exactQuoteStatus=UNAVAILABLE_NO_CONTRACT`
   - 阻塞码：`SUPPLIER_QUOTE_DISABLED`、`SUPPLIER_QUOTE_HTTPS_REQUIRED`、`SUPPLIER_EXACT_QUOTE_CONTRACT_UNAVAILABLE`
3. 发布准备服务要求候选绑定真实、未过期、精确到岸价的 `VERIFIED/PASS` 利润证明。当前不能通过关闭或放宽该门禁来让真实 smoke 变绿。

### 2026-07-17 补充决议复核

- 已确认补充决议允许在登录 401 时通过公开注册接口创建隔离 smoke 组织；该方向可实现，但尚未执行，因为人工定价路径出现了决议中约定的硬阻塞。
- `PATCH /review/:id/manual-pricing` 当前只把 `manual-pricing-evidence/v1` 写入 `review_tasks.decisionEvidence.manualPricing` 并追加审计日志；该事务没有创建或更新 `CandidateEconomicsEvaluation`、`SupplierQuoteEvidence` 或发布利润证明。
- `ProductLaunchService` 和 `ListingPublishSnapshotService` 均不读取 `manualPricing`。每日候选的 `PUBLISH_READY` 准备仍调用 `CandidateEconomicsPublishProofService`。
- `CandidateEconomicsPublishProofService` 强制要求绑定 `supplierQuoteEvidenceId`，且供应商报价必须满足 `verificationStatus=VERIFIED`、`priceKind=EXACT`、`shippingScope=LANDED_RU`。人工定价证据不能满足该合同。
- `AWAITING_ECONOMICS_REVIEW` 没有恢复或晋级 API；Worker 对该状态只返回当前图片和 Listing，本身不会创建发布审核任务。
- `confirmPublish` 只接受 `AWAITING_PUBLISH_APPROVAL`，因此不能使用人工定价证据到达补充决议要求的真实终点。

结论：补充决议明确要求“若人工定价路径不支持则停下说明，不得绕过门禁”。现已按该规则停止；没有修改利润门禁、风险门禁、发布证明或契约。

### 待产品决议

- 方案 A：单独立项实现“人工定价证据 → 可验证候选利润评估 → 发布快照证明”的完整合同、校验、审计和测试，再继续 E-0。
- 方案 B：把真实 E-0 的终点改为 `AWAITING_ECONOMICS_REVIEW`，只验收真实图片与本地 Listing，不声称已到发布审批门禁。
- 方案 C：恢复原精确供应商报价方向，接入合法 `EXACT + LANDED_RU` 合同。

选择方案前，不继续实现 `ensureAccount`、测试图片上传，也不进入 E-1。

在以上真实前置条件满足前，E-0 不标记完成，也不进入 E-1。

## E-1 至 E-5

状态：未开始。按任务书要求等待 E-0 完整验收通过。
