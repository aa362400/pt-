-- Replace this template with the reviewed rollback SQL for the migration.
-- If rollback is unsafe, set rollbackMode=FORWARD_ONLY in metadata.json and
-- document the exact forward-fix and recovery procedure below.

-- FORWARD_ONLY_RECOVERY: not yet defined
