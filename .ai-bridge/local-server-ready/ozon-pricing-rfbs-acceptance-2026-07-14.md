# Ozon 核价与 rFBS 退款链路严格验收

- 验收日期：2026-07-14
- 判定：`CONDITIONAL_READY`
- 企业级总门禁：`FAILED`
- 外部店铺写入：未执行

## 已真实通过

| 验收项 | 证据 | 状态 |
|---|---|---|
| Ozon 售价表导入 | `售价表260604.xlsx` 已解析 1 条有效数据、3366 条空行、0 条无效数据；源文件 SHA-256 为 `a27ba46d5ff5332b23bbde3cda359da90007c4aaf4b73b351acbe4d164b39ff7` | PASS |
| 核价计算 | 导入结果为销售价 ¥227.45 / ₽2565.49，利润 ¥45.49，利润率 20%；计算记录 `cmrk31f6q00hnpe01can78jh6`，审计序号 14 | PASS |
| 核价前端 | Ozon 核价页支持 Excel 上传、字段预览、计算结果和证据摘要 | PASS |
| rFBS 只读接口 | 已实现列表、详情、商品、状态和可用动作的安全映射；不返回客户姓名等 PII | PASS（自动化） |
| rFBS 退款申请 | 前端只能创建人工审核任务，不直接调用 Ozon 写接口 | PASS |
| rFBS 退款执行 | 仅支持 `ozon.order.refund`、rFBS 全额退款、明确确认、租户所属店铺、严格审计、执行后读回校验和幂等处理 | PASS（自动化） |
| 权限门禁 | 提交退款审核仅限 OWNER/ADMIN；无店铺、无退款动作或未勾选确认时按钮禁用 | PASS |
| 后端全量测试 | 77 个套件通过、1 个跳过；447 个测试通过、2 个跳过 | PASS |
| 后端构建 | NestJS 生产构建通过 | PASS |
| 前端构建 | TypeScript/Vite 生产构建通过 | PASS |
| 前端静态检查 | 退出码 0，仅保留既有未使用导入警告 | PASS |
| 本地服务健康 | `/api/v1/ready` 返回 200；database、redis、storage、agent 均为 `up` | PASS |
| 差异检查 | `git diff --check` 退出码 0；仅有 LF/CRLF 提示 | PASS |

## 当前不能标记通过

| 验收项 | 实际情况 | 状态 |
|---|---|---|
| 当前数据库真实 Ozon 店铺 | 本次重建后的 `channel_connections` 为 0，当前浏览器账号显示 0 个 Ozon 店铺 | BLOCKED_EXTERNAL |
| rFBS 真实退货列表复验 | 没有已连接店铺，无法调用真实 Ozon rFBS 列表；空页面不是成功证据 | BLOCKED_EXTERNAL |
| 真实退款写入 | 未发现可验证 rFBS 退货样本，且真实退款是不可逆财务动作；未执行 | BLOCKED_EXTERNAL |
| 退款后真实读回 | 因未执行真实退款，不能证明真实平台最终状态已改变 | BLOCKED_EXTERNAL |

## 企业级总门禁

当前通过：

- 非 Mock Agent：最近 3 个完成任务。
- MCP 信任：Ed25519 签名、manifest、可执行文件和工具集合匹配。
- 记忆治理：62/62 带治理元数据。
- Ozon 只读回归：通过。

当前阻断：

1. AWS KMS 未配置。
2. S3 Object Lock + SSE-KMS 审计归档未配置。
3. 缺少可验证的外部渗透测试报告。
4. 14 天 SLO 窗口不完整；最少观测 2 天，通过天数 0。
5. Judge 回归和 live judge 已通过，但缺少授权人工金标签名。
6. Stripe live 支付/退款凭证缺失。

## 允许的发布范围

- 允许：本地 Ozon 核价、Excel 导入、只读商品/订单/退货查询、生成报告、创建人工审核任务。
- 受控允许：重新绑定真实 Ozon 店铺后进行 rFBS 只读列表和详情验收。
- 禁止自动：商品发布、改价、库存、广告、退款和其他外部写入。
- 真实 rFBS 退款必须逐单展示证据并由用户在动作发生时单独确认。

## 下一步复验条件

1. 在当前本地数据库重新绑定 Ozon 店铺。
2. 使用真实 rFBS 退货样本验证列表和详情，记录安全摘要，不保存 PII。
3. 用户针对具体退货单确认后，才可提交审核并执行一次真实全额退款。
4. 核对 Ozon 读回状态、审计链、通知状态和前端同步。
5. 企业级总判定仍需补齐 6 个外部门禁，不能用本地测试替代。
